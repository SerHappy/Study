#include <iostream>

using namespace std;

int main()
{
    string choice;
    int a;
    cin >> a;
    cout << "a^3 = " << a * (a*a) << endl;
    //cout << "a^10 = " << (a*a) * (a*a) << endl;
    cout << "a^4 = " << (a*a) * (a*a) << endl;
    cout << "a^5 = " << (a*a) * (a*a) * a << endl;
}